[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
commands = ["npm ci"]

[phases.build]
commands = ["npm run build"]

[start]
cmd = "npm run start"

[environment]
NEXT_TELEMETRY_DISABLED = "1"
